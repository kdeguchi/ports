--- src/MODULE.bazel
+++ src/MODULE.bazel
@@ -127,6 +127,12 @@ pkg_config_repository(
     ],
 )
 
+# uim
+pkg_config_repository(
+    name = "uim",
+    packages = ["uim"],
+)
+
 # Qt for Linux
 pkg_config_repository(
     name = "qt_linux",
