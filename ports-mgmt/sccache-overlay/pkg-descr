Ports overlay for sccache

Sccache is a compiler cache but unlike ccache it comes with Rust
support. This package provides an overlay for the ports framework
to allow sccache's client-server model to work properly.
